{{ $galleryId := .Get "id" | default "gallery" }}
<div class="gallery" style="display: grid; grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); gap: 8px; margin: 1rem 0;">
  {{ range .Page.Resources.Match (printf "%s/*" $galleryId) }}
    <a href="{{ .RelPermalink }}" class="glightbox" data-gallery="{{ $galleryId }}">
      {{ $thumb := .Resize "300x" }}
      <img src="{{ $thumb.RelPermalink }}" alt="{{ .Name }}" style="width: 100%; height: 150px; object-fit: cover; border-radius: 4px; cursor: pointer;">
    </a>
  {{ end }}
</div>
